expname: reconstuct_and_sample_total_dataset_max_teacher_forcing

# metadata specialised for each experiment
core:
  version: 1.0.0
  tags:
    - ${now:%Y-%m-%d}

hydra:
  run:
    dir: ${oc.env:HYDRA_JOBS}/singlerun/${now:%Y-%m-%d}/${expname}/

defaults:
  - data: data_combined
  - model: diffusion
  - optim: optim
  - train: train
  - logging: wandb_logging