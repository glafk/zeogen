expname: final_base_cdvae_100e_small_data_lt50

# metadata specialised for each experiment
core:
  version: 0.0.1
  tags:
    - ${now:%Y-%m-%d}

hydra:
  run:
    dir: ${oc.env:HYDRA_JOBS}/singlerun/${now:%Y-%m-%d}/${expname}/

defaults:
  - data: data_small
  - model: diffusion
  - optim: optim
  - train: train
  - logging: wandb_logging
