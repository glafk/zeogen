expname: fit_scaling_all_codes_lt50_frame_cond

# metadata specialised for each experiment
core:
  version: 0.0.1
  tags:
    - ${now:%Y-%m-%d}

hydra:
  run:
    dir: ${oc.env:HYDRA_JOBS}/singlerun/${now:%Y-%m-%d}/${expname}/
  
defaults:
  - data: data_small
  - model: diffusion
  - optim: optim
  - train: train